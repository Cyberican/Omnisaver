# pyproject.toml
[project]
name = "omnisaver"
version = "0.1.0"
description = "File/process automation utilities"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["omnisaver*"]
